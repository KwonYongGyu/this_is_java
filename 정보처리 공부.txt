mar (memory address register, 메모리 주소 레지스터) : cpu가 데이터를 읽거나 쓰려는 메모리의 주소를 일시적으로 보관하는 레지스터
mbr(memory buffer registor, 메모리 버퍼 레지스터) : 메모리로부터 읽어온 데이터나 메모리에 저장할 데이터를 일시적으로 보관하 창구 역할
ixr(Index Registor, 인덱스 레지스터) : 주소 지정 방식에서 주소를 변경(인덱싱)하거나 반복 계산의 횟수를 제어할 때 사용하는 레지스터

마이크로 연산들의 집합을 마이크로 명령어라고 한다.(마이크로 프로그래밍은 아니다.)
간접 사이클(Indirect Cycle)에서는 데이터를 가지고 있는 주기억 장치의 유효주소를 찾는다.

R2 <- R3+R4는 마이크로 명령어의 한 예로서 R3과 R4 레지스터의 내용을 가산하여 R2 레지스터에 저장하는 동작이다.

Direct mapping : 가상 기억 장치에서 가져오는 블록을 페이지라고 하고 그 페이지 번지 변환 기법의 종류 중 하나이다.
현재 수행 중인 프로세스가 가상 번지를 참조하여 페이지 사상표 시작점 레지스터내의 시작번지를 페이지에 더해 페이지 사상표 내의 번지를 얻는다. 이어 가상 페이지에 접속하여 실 번지를 얻는것


segment 기법은 캐시 메모리가 아닌 가상 메모리에서 관리 기법이다.

캐시에 기억시키는 블록 주소의 일부는? > 태그주소

프로그램 실행 도중 분기(Branch)가 발생했다면 다음 실행할 명령의 주소를 가지고 있는 PC의 내용이 변경된 것이다.

Poling : 소프트웨어, 직렬연결
Daisy Chain : 하드웨어, 직렬 연결


번지 필드(Address Field)가 필요 없는 명령은 스킵(Skip)명령이다.

교착상태 예방 방법을 Prevention이라 하며 상호배제 부정 부분할당 부정 비선점 부정 환형대기 부정 등이 있으며 
Avoidance는 회피 Detection는 발견 recovery는 회복을 뜻한다.

상호 배제 : 하나의 프로세스가 공유 자원의 사용을 완료할 때까지 다른 프로세스의 공유 자원에 대한 접근을 금지하도록 하는 제어 기법

운영체제의 성능 평가 기준
Throughout - 처리능력
Reliability - 신뢰도(주어진 문제를 정확하게 해결)
Turn Around Time - 응답시간
Avilability - 사용 가능도

RR(Round Robin) 방식은 선점 스케줄링 방식의 하나로, 4번(프로세스에게 이미 할당된 프로세서를 강제로 빼앗을 수 없고, 그 프로세스의 사용이 종료된 후에 스케줄링 해야하는 방법을 택하고 있다.)은 비선점 스케줄링에 대한 설명이다. 시분할 시스템에 해당한다.


순차파일은 구조가 간단해 특정 레코드를 검색하기는 어렵다.

유닉스에서 쉘이 해석하면 커널은 수행한다.
쉘(shell) : 사용자의 명령어를 인식하여 프로그램을 호출하고 명령을 수행하는 명령어 해석기

주/종 구조
주(Master)는 입출력을 담당하며 운영체제의 핵심 기능은 주 프로세서만 관리한다. 자원 할당이나 스케줄링 같은 대장 노릇은 주 프로세서의 몫이다. 주 프로세서가 고장나면 모든 시스템이 다운된다.

인터럽트 발생 시 
- PC의 내용을 보관
- 인터럽트 처리 루틴의 수행
- 인터럽트가 발생된 장치를 추적
- 수행중인 프로그램은 보관하지 않는다.

인터럽트 수행 후
- 인터럽트 처리 시 보존시켰던 PC 및 제어상태 데이터를 PC와 제어상태 레지스터에 복구한다.

가중치 코드는 각 비트 위치에 따라 값이 정해진 코드이다.
비가중치 코드는 각 위치에 해당하는 값이 없는 코드이다.

[임계 구역 문제의 해결 조건]
상호 베제 : 하나의 프로세스가 임계구역에 들어가 있다면 다른 프로세스는 들어갈 수 없음
진행 : 임계 구역에 들어간 프로세스가 없는 상태에서 들어가려고 하는 프로세스가 여러 개 있을 경우 어느 것이 들어갈 건지를 정해준다.
한계 대기 : 하나의 포르세스가 들어가서 오랫동안 나오지 않으면 다른 프로세스는 실행시킬 수 없으므로 이러한 기아(Starvation)을 방지하기 위해 한 번 임계구역에 들어간 프로세스가 다음 번 들어갈 때 제한을 해준다.

[운영체제의 역할]
- 사용자와의 인터페이스 구현
- 프로세서, 메모리 등의 자원 스케줄링
- 입, 출력을 위한 편의 제공

전화와 텔레비전의 연결에 의한 정보서비스의 형태 : 비디오텍스

ASCII 코드를 수신했을 경우, 우수 패리티를 사용하여 에러를 검출할 수 있는 경우는 1이 홀수인 경우를 찾으면 된다.

데이터 교환 방식은 축적 교환 방식과 회선 교환 방식으로 나뉜다.

[인터럽트 발생 원인]
- 입출력 인터럽트 : 자료의 연산이 종료된 후에 발생한다.
- 프로그램 오류 인터럽트 : 프로그램의 실행도중에 연산 오류나 번지 착오 등의 오류가 있을 때 발생한다.
- SVC 인터럽트 : 프로그램에서 의도적으로 SVC(SuperVisor Call) 명령을 사용하는 경우에 발생한다.
- 외부 인터럽트 : 할당된 시간을 초과하거나, 콘솔(Console)을 이용하여 오퍼레이터가 중단 키를 누를때, 또는 CPU 간에 통신할 때 등에 발생한다.
-- 부프로그램 호출은 포함되지 않는다.


DBCDIC코드 : 각각의 문자에 대하여 8개의 비트와 1개의 패리티 비트로 구성되는 코드이다.

staging : 가상기억체제에서 page fault가 발생하면 희생 페이지를 결정해서 보조기억장치의 이전 위치에 기억시키고 새로운 페이지를 이전 희생된 페이지가 있던 곳에 위치시키는 것

스레싱(Thrashing) : 가상기억장치의 경영에서 어떤 프로세서가 충분한 프레임을 갖기 못하여 페이지 교환이 계속적으로 발생하여 전체 시스템의 성능이 저하되는 것

ATM 셀의 총 길이는 53byte이고 헤더는 5byte 몸통은 48byte이다.

잡음에 가장 민감한 변조방식은 ASK 진폭변조 방식이다.

위성통신은 광대역 통신, 대용량, 고품질의 정보 전송이 가능하다. 전파 지연이 있고 감쇄현상이 발생한다.

소프트웨어의 생명 주기(SDLC : Software Development Life Cycle)
1. 폭포수 모형 : 가장 오래된 생명주기 모형으로 적용 사례가 많고 요구 사항 변경이 어려움
2. 프로토타이핑 모형 : 폭포수 모형의 단점을 보완한 것으로, 사전에 시제품을 미리 만들어 사용자의 요구를 정확하게 판단하는 것이다.
3. 나선형 모형 : 폭포수와 프로토타입 모형의 장점만 수용한 것, 사용자의 초기 요구 사항에 근거한 위험 요소들을 관리 기술을 통해 해결하는 모형

Go-back-N ARQ : 송신 스테이션이 데이터 프레임을 연속적으로 전송하다가 NAK를 수신하게 되면, 에러가 발생한 프레임 이후에 전송된 모든 프레임 이후에 전송된 모든 프레임을 재전송하는 방식

광대역 종합정보통신망의 실형 방안으로 적합한 통신방식 : ATM

[프로토콜의 기본 요소]
구문(Syntax), 의미(Semantics), 타이밍(Timing)

부동소수점 표현의 수치 자료 2개에 대하여 합산을 할 때 두 자료의 지수 베이스(base)는 같고, 지수 크기가 다르다면 지수를 어느 쪽에 일치시켜 계산해야 하는가? => 지수가 큰 쪽에 일치시킨다.

전자계산기는 대별해서 중앙처리 장치와 주변장치로 구분한다. 여기서 중앙처리장치의 구성 성분은?
Control, Memory, Arithmetic

캐시 기억장치는 성능을 향상시키기 위해 주기억 장치와 CPU 레지스터 사이에서 데이터를 이동시키는 중간 버퍼로 작용한다.

절대로더(Absolute loader) : 프로그램과 데이터를 실행하기 위해 절대 주소 양식으로 기억 장소에 적재되는 루틴. 
명령의 주소부가 모두 절대 주소로 표현되어 있는 프로그램을 적재하는 프로그램이다.

인터넷 응용서비스에서 가상 터미널(VT) 기능을 갖는 것 : TELNET

HDLC는링크 구성 방식에 따라 세 가지의 동작 모드를 가지고 있다. 
- NRM, ARM, ABM                  SBM은 아니다.

패킷교환 방식 : 패킷 전송은 음성전송보다 데이터 전송에 더 적합하다.

시스템 카탈로그 : 시스템 자신이 필요로 하는 여러 가지 객체에 관한 정보를 포함하고 있는 시스템 데이터베이스로서, 포함되고 있는 객체로는 테이블, 데이터베이스, 뷰, 접근 권한 등이 있는 것

속성(Attributes) : 각 개체가 가지고 있는 특정한 성질

[E-R 모델]
사각형은 개체타입을 의미
마름모는 관계
타원, 원은 속성

3주소 명령어
- operand부가 3개로 구성되며, 연산의 결과는 operand 3에 기록된다.
- 연산 후에 입력 자료가 변하지 않고 보존된다.
- 프로그램의 길이는 짧아지지만, 명령어 한 개의 길이가 길어진다.

한 명령을 두 부분으로 나누면 operation과 operand이다.(번지와 데이터는 아니다.)

데이터 레지스터 : Stack, Accumulator, General Purpose Registor
프로그램 카운터는 CPU제어장치에 속한다.

링키지 에디터(Linkage Editor) : 프로그램 적재시에 필요한 프로그램들을 결합하여 주기억장치에 적재함은 물론 보조기억장치에 로드 이미지를 보관해 두는 역할을 함

세마포어(Semaphore) : 동기화를 구변할 수 있는 기법 중 하나, 각 프로세서에 제어 신호를 전달하여 순서대로 작업을 수행하도록 하는 기법

[시스템 소프트웨어]
- 프로그램을 주기억장치에 적재, 시스템 전체를 작동시키는 프로그램, 인터럽트 관리, 장치 관리 등의 기능을 담당

복잡한 수학계산을 처리하는 것은 스프레드시트 + 응용소프트웨어이며 엑셀에서 함수계산한다고 생각하면 쉽게 풀 수 있다.

디스크 스케줄링 기법 중 SSTF(Shortest Seek Time-First)는 FCFS(Fist-come-fist-served)보다 처리량이 많고 평균 응답시간이 짧다.

핸드오프(Hand Off) : 이동통신망에서 통화 중인 이동국이 현재의 셀에서 벗어나 다른 셀로 진입하는 경우, 셀이 바뀌어도 중단 없이 통화를 계속할 수 있게 해준다.

정보통신망(전산망)에서 상호간을 연결할 때 시설, 운영 및 유지, 보수의 책임한계를 구분하기 위한 접속점을 "분계점"이라고 한다.

주소 필드 : 프레임을 송신, 수신하는 스테이션을 구별하기 위해 사용되는 스테이션 식별자 필드

선형 자료구조
- List
- Queue
- Stack

자료구조의 영역(data area)을 편성하는 방법에서 File 내의 각 item을 논리적인 순서에 따라 물리적으로 연속된 위치로 저장하는 방법은? - Sequential(순차) 편성

Random 편성 (직접 편성)
레코드의 키 값을 기반으로 해싱(Hashing) 함수를 사용하여 데이터가 저장될 물리적 주소를 직접 계산하는 방식입니다.

HDLC(High-Level Data Link Control)에 사용되는 프레임 종류 : 정보 프레임, 감독 프레임, 무번호 프레임 -- 제어 프레임은 사용되지 않는다.

HDLC : 비트지향형 프로토콜이며 링크 구성 방식에 따라 세 가지 동작모드를 가지고 있다. 데이터 링크 계층의 프로토콜이다.

데이터베이스 관리 시스템(DMBS)

로킹 단위가 크면 병행성수준이 감소한다.

가상기억장치에 사용되는 보조기억장치는 하드디스크를 많이 사용한다.


SQL 명령문 : Distinct라는 구문은 중복제거를 의미한다.

입출력 장치와 주기억장치 사이에 데이터 전송을 담당하는 입출력 전담 장치는 "채널 장치"이다.

채널은 MODEM의 기능을 갖지 않는다. I/O 속도를 향상시키며 고속 방식과 저속 방식의 채널이 있다. 도한 CPU의 idle time을 줄인다.

출력 설계 단계 중 출력 정보 분배에 대한 설계 시 검토 사항으로 거리가 먼 것은? -> 출력 정보명
출력 정보명은 출력 정보에 대한 설계 시 검토 사항이다.

스레드는 하드웨어적 접근이 아닌 소프트웨어적 접근 방법이다.

계층화 구조의 기본 구성 요소 : 개체, 데이터 단위, 접속  -- 주소는 포함되지 않는다.

Li-Fi : 발광다이오드(LED)가 방출하는 전파를 이용해 데이터를 주고 받는 가시광 무선통신 기술 빛이 있는 곳이면 어디든 사용 가능, 새로운 무선 통신 기술로 평가 받고 있다.
69번 다 풀고 확인
77 80 83 85 86 87 93

--------------------------------------------------------25년도 1회 필기 -------------------------------------------------------------------
1. 1 	2. 2	3. 2	4. 4	5. 4	6.3	7.2	8.1	9. 2	10. 1	11. 3	12. 3	13. 2	14. 3	15. 3	16. 3	17. 1
18. 4	19. 2	20.4	21. 2	22. 3	23. 4	24. 1	25. 4	26. 4	27. 2 28. 1	29. 1	30. 4

1, 2, 4, 5, 9, 10, 11, 12, 14, 17, 18, 20, 25, 27, 28, 30