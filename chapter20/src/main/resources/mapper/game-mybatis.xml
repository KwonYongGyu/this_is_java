<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mjc813.chapter20.gamecrud.IGameMybatis">
	<insert id="insertData" parameterType="GameDto">
		INSERT INTO game_tbl (
			name
			, genre
			, grade
			, price
			, img_url
		) VALUES (
			#{name}
			, #{genre}
			, #{grade}
			, #{price}
			, #{imgUrl}
		)
	</insert>

	<update id="updateData" parameterType="GameDto">
		UPDATE game_tbl
		SET name = #{name}
			, genre = #{genre}
			, grade = #{grade}
			, price = #{price}
			, img_url = #{imgUrl}
		WHERE id = #{id}
	</update>
	<select id="selectTotalCount" parameterType="SearchDto" resultType="int">
		SELECT COUNT(*)
		FROM game_tbl
		<where>
			<if test="searchName != null and searchName != ''">
				AND name LIKE CONCAT('%', #{searchName}, '%')
			</if>
			<if test="searchGenre != null and searchGenre != ''">
				AND genre = #{searchGenre}
			</if>
			<if test="searchGrade != null and searchGrade != ''">
				AND grade = #{searchGrade}
			</if>
		</where>
	</select>

	<select id="selectList" parameterType="SearchDto" resultType="GameDto">
		SELECT
		id
		, name
		, genre
		, grade
		, price
		, img_url
		FROM game_tbl
		<where>
			<if test="searchName != null and searchName != ''">
				AND name LIKE CONCAT('%', #{searchName}, '%')
			</if>

			<if test="searchGenre != null and searchGenre != ''">
				AND genre = #{searchGenre}
			</if>
			<if test="searchGrade != null and searchGrade != ''">
				AND grade = #{searchGrade}
			</if>
		</where>
		ORDER BY id DESC
		<!--페이징 처리 추가 -->
		LIMIT #{recordSize} OFFSET #{offset}
	</select>

	<delete id="deleteData" parameterType="GameDto">
		DELETE FROM game_tbl
		WHERE id = #{id}
	</delete>
</mapper>
