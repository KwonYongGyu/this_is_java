spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3307/toeic_db
    username: kyg
    password: kyg1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
